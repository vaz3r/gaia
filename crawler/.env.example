# WireGuard client secrets for the crawler Docker stack.
# Copy to .env and fill in real values. NEVER commit .env.
WIREGUARD_PRIVATE_KEY=
WIREGUARD_PRESHARED_KEY=
SERVER_PUBLIC_KEY=

# Postgres credentials for the platform stack.
POSTGRES_PASSWORD=
